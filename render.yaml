services:
  - type: web
    name: flashcard-learning
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server
    envVars:
      - key: PORT
        value: 8000
